{
  "description": "Controller config for the bounded Hell Week tuning arc. The controller reads these as enforced ceilings/fields and aborts when one is hit; they are not advisory. Pairs with workflows/hell-week-tune.md.",
  "iterationCeiling": 6,
  "candidatePatchCeiling": 8,
  "tokenCeiling": null,
  "baselinePin": "artifacts/evidence-index/baseline.json",
  "disjointOwners": ["retrieval", "planner", "validator", "state", "corpus", "scenario"],
  "oneOwnerPerSlice": true,
  "smokeBeforeFull": true,
  "stabilityMinRuns": 3,
  "keepStatuses": ["REPAIRED", "HOLDING"],
  "promotionRequires": "REPAIRED",
  "firstArc": {
    "calibration": true,
    "alwaysNotify": true,
    "iterationCeiling": 2,
    "note": "Run the first arc with every safety/ambiguity/inconclusive trigger forced to notify, to tune thresholds against real signal before trusting silence."
  }
}
